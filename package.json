{
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "chrome-types": "^0.1.439",
    "eslint": "^10.8.1",
    "jsdom": "^29.1.1",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.67.0",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build:login-fields": "vite build --config vite.login-fields.config.mts",
    "build:fill-credentials": "vite build --config vite.fill-credentials.config.mts",
    "build:assets": "cp manifest.json dist/manifest.json && cp src/popup/popup.html dist/popup/popup.html && cp -r src/style dist/style && cp -r src/img dist/img",
    "build": "npm run clean && tsc && npm run build:login-fields && npm run build:fill-credentials && npm run build:assets",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run"
  }
}
